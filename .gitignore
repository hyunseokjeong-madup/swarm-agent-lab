# OS / editor
.DS_Store
Thumbs.db
*.swp
.idea/
.vscode/

# Python
__pycache__/
*.pyc
.venv/
venv/

# Scratch / temp
scratchpad/
*.tmp
*.log

# Large regenerable benchmark artifacts (recreate via marketing/bench/gen_dataset.py)
marketing/bench/dataset.csv
marketing/bench/ground_truth.json

# FTS5 search index — regenerable via `python marketing/knowledge/search.py --build`.
marketing/knowledge/.index.db

# Tool-generated HTML output written to the working dir (dashboard.py / cohort_heatmap.py).
# The intended demo versions live in docs/demo/ (committed); only ignore stray root-level ones.
/*.html
marketing/*.html
marketing/pm/*.html

# Keep results/ (reproducibility evidence) — do NOT ignore

# pytest cache (madobi 는 stdlib run_all.py 를 쓰지만, 누가 pytest 돌리면 생긴다)
.pytest_cache/
